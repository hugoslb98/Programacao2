#include <stdio.h>
#include <stdlib.h>


int main(int argc, char** argv) {
    int min, km, posto1, posto2, posto3;
    
    puts("Em quanto tempo pretende fazer a viagem ? ");
    scanf("%d", &min);
    
    if (km <= 60) {
        
    }
    return 0;
}

